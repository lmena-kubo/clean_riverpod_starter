pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.dart"
      run: fvm dart format {staged_files}
      stage_fixed: true
    analyze:
      glob: "*.dart"
      run: fvm flutter analyze

pre-push:
  commands:
    test:
      run: fvm flutter test
